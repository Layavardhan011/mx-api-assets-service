apps:
  api:
    port: 3004
    privatePort: 4004
    useCachingInterceptor: true
  cacheWarmer:
    port: 4100
  queueWorker:
    port: 4002
  transactionsProcessor:
    port: 4003
    maxLookBehind: 100
libs:
  common:
    redis:
      host: ${REDIS_HOST}
      port: ${num:REDIS_PORT}
      password: ${REDIS_PASSWORD}
    nativeAuth:
      # maxExpirySeconds:
      acceptedOrigins:
        - utils.multiversx.com
    security:
      admins: []
    # rateLimiterSecret:
